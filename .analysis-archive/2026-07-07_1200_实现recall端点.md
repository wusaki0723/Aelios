```
<analysis>
context: src/api/memories.ts 已有 handleSearchMemories（185-218 行）：解析 query/namespace/top_k/types/filter/include_prompt，调用 searchMemories + filterAndCompressMemoriesWithMeta，不经 v2 runRecall。handleMemories（861 行）路由 tail[0]==="search" POST 分支。isV2Enabled/runRecall 在 src/memory/v2/recall.ts；MCP memory_recall 已调用 runRecall。formatMemoryPatch（inject.ts:85）仅使用 content、importance、pinned、type 四字段。RecallHit 含 id/content/type/score/source/source_layer，无 importance。wrangler.toml [vars] 有 MEMORY_TOP_K=50，无 MEMORY_INJECT_DECAY_FACTOR。README 端点表在 211 行记录 POST /v1/search/memories。
needs: 新增 POST /v1/memory/recall 走 runRecall 三闸去重与 last_injected_at 记账；v2 关闭时回退 handleSearchMemories；wrangler 设 MEMORY_INJECT_DECAY_FACTOR=0.15；README 文档；构建报告；tsc 通过；不改 handleSearchMemories。
key_challenges: k 解析需 k 优先、top_k 别名、默认 MEMORY_TOP_K；min_score 仅有限数透传；include_prompt 仅在 data.length>0 时输出；formatMemoryPatch 需 MemoryApiRecord 类型但运行时只需四字段；glossary_hits 追加格式。
confidence: HIGH
  - HIGH: 侦察充分，MCP memory_recall 为参考实现，边界条件已从 spec 明确
approach: 在 memories.ts 新增 handleRecallMemories + recall 路由分支；扩展 isV2Enabled 导入为 runRecall；映射 hits 并 cast 给 formatMemoryPatch；glossary 行追加；wrangler.toml 加 decay factor；README 表行+短节。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 与 handleSearchMemories/MCP 模式一致，复用 formatMemoryPatch
  - 健壮性: 5/5 — v2 回退、query 校验、未知字段忽略、min_score 有限数守卫
  - 可扩展性: 4/5 — 响应 meta 扩展 result.meta，hook 可切路径
edge_cases:
  - query 缺失或空字符串返回 400
  - isV2Enabled false 时整请求委托 handleSearchMemories（含 filter/include_filter_debug）
  - k/top_k 非数字时用 MEMORY_TOP_K 默认 50，runRecall 内部钳 1..100
  - min_score 非有限数不传 runRecall（用 env 默认）
  - include_prompt true 但 data 空时不返回 prompt 字段
  - glossary_hits 非空时在 memory patch 后追加 - [glossary] term: definition
  - body 含 filter/include_filter_debug 等未知字段不报错
affected_scope:
  - /home/ubuntu/claude-workspace/aelios/src/api/memories.ts
  - /home/ubuntu/claude-workspace/aelios/wrangler.toml
  - /home/ubuntu/claude-workspace/aelios/README.md
execution_plan:
  - step_1: 在 memories.ts 导入 runRecall 与 MemoryApiRecord，实现 handleRecallMemories（scope、body 解析、v2 分支、响应映射、prompt+glossary）
  - step_2: 在 handleMemories search 分支旁添加 recall POST 路由
  - step_3: wrangler.toml [vars] 添加 MEMORY_INJECT_DECAY_FACTOR 及中文注释
  - step_4: README 在 search 端点附近添加 /v1/memory/recall 小节
  - step_5: 运行 npx tsc --noEmit，grep 验证路由，写 RECALL_BUILD_REPORT.md
degradation_check:
  - 方案是否是三维评估综合最优的？ → YES — 可维护性 5、健壮性 5、可扩展性 4，无更优替代
  - 是否遗漏了已知边界条件？ → NO — spec 列出的边界均在 edge_cases
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO — 三文件全覆盖
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？是否有未读但可能相关的文件？ → YES — inject.ts formatMemoryPatch、mcp memory_recall、RecallHit 已读
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
```
---
```
<decision_point>
issue: npx tsc --noEmit 报错 TS2352：`data as MemoryApiRecord[]` 类型不重叠，因响应 data 映射形状缺少 MemoryApiRecord 必填字段。
impact: YES — 不修复则验收失败
context_update: formatMemoryPatch 虽运行时只需四字段，但 TypeScript 要求完整 MemoryApiRecord；需在 prompt 路径填充安全默认值而非单 cast。
options:
  - option_a:
      description: 为 formatMemoryPatch 单独构建 promptRecords，填充 namespace/summary/pinned 等安全默认值
      approach_evaluation: 可维护性 5/5（符合 spec「fill safe defaults」）；健壮性 5/5；可扩展性 4/5
      edge_cases: 空 content 经 sanitize 后 patch 可能为空，glossary 行仍可追加
      affected_scope_delta: 无新文件
  - option_b:
      description: 使用 `as unknown as MemoryApiRecord[]` 双 cast
      approach_evaluation: 可维护性 3/5；健壮性 3/5（绕过类型检查）；可扩展性 3/5
      edge_cases: 运行时缺字段若 formatMemoryPatch 扩展会静默失败
      affected_scope_delta: 无
  - option_c:
      description:  fork 新 formatter 专供 recall
      approach_evaluation: 可维护性 2/5；健壮性 4/5；可扩展性 2/5
      edge_cases: 与 inject 路径格式漂移
      affected_scope_delta: 可能需 inject.ts（违反 spec 范围）
recommendation: option_a — 三维评估综合最优，且符合 spec 指引
execution_plan_update: step_1 中 prompt 路径改为 promptRecords 映射后调用 formatMemoryPatch
deviation_audit:
  original_plan_excerpt: step_1: 映射 hits 并 cast 给 formatMemoryPatch
  current_proposal: step_1: 映射 hits；prompt 路径用 promptRecords 填充 MemoryApiRecord 默认值
  diff_summary: 将单 cast 替换为显式 promptRecords 构建
  deviation_motive_check:
    - 措辞替换规则逐类检查：全部未命中
    - 偏离后的方案在三维评分上是否低于原方案？→ NO，可维护性/健壮性更高
    - 偏离是否导致 affected_scope 缩小？→ NO
degradation_check:
  - 推荐方案是否是三维评估综合最优的？ → YES
  - 推荐方案是否遗漏了新发现的边界条件？ → NO
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO（选了更完整方案）
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES
  - 是否有发现了但被判断为无关紧要而跳过的问题？ → NO
  - options 是否包含至少三个方案？ → YES
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO
  - deviation_audit 是否触发了 self_dissection？ → NO
</decision_point>
```
---
```
<output_quality_review>
task_summary: 按 RECALL_ENDPOINT_SPEC 实现 POST /v1/memory/recall，含 v2 runRecall 管线、wrangler decay factor、README 文档与构建报告
deliverables:
  - src/api/memories.ts (handleRecallMemories + route)
  - wrangler.toml (MEMORY_INJECT_DECAY_FACTOR)
  - README.md (recall subsection)
  - RECALL_BUILD_REPORT.md

metrics:
  total_files_modified: 3 — spec 允许范围内全部完成
  execution_plan_coverage: 5/5 = 100%
  edge_cases_handled: 7/7 = 100%
  confidence_assessment: HIGH
    - HIGH: tsc 通过，路由 grep 确认，handleSearchMemories 未改，构建报告已写

substance_check:
  - 产物中是否存在形式完整但实质空洞的内容？ → NO — handler 调用 runRecall 并映射响应，decay factor 影响闸三行为
  - 产物是否能被目标变化击穿？ → YES — 删除 recall 路由或 runRecall 调用会 grep/tsc/行为失效
  - 实质性比率: 4/4 = 100%

completeness_check:
  - 是否存在被跳过的模块？ → NO
  - 产物覆盖与 affected_scope 一致？ → YES（三文件 + 报告路径）
  - 核心业务逻辑有直接验证？ → YES — tsc + grep 路由 + diff 确认 search 未改
  - affected_scope 覆盖率: 3/3 = 100%

value_density_check:
  - 高价值:低价值 = 4:0，高价值占比 100%
  - 是否存在用数量掩盖质量？ → NO

alignment_check:
  - 满足本质需求：hook 可走 recall 端点触发三闸去重与 last_injected_at 记账
  - 如果这是别人交给我的，我会接受吗？ → YES
</output_quality_review>
```